angular.module('laboard-frontend')
    .controller('HomeController', [
        '$rootScope', '$scope', '$modal', '$state', 'ProjectsRepository',
        function ($root, $scope, $modal, $state, $projects) {
        }
    ]);
