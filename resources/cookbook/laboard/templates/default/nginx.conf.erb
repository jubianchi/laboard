server {
  listen *:80;
  server_name <%= @domain %>;

  location / {
    proxy_pass http://127.0.0.1:<%= @port %>/$request_uri;
    proxy_redirect http://localhost:<%= @port %>/ /;
  }

  location ~^/.+ {
    proxy_pass http://127.0.0.1:<%= @port %>$request_uri;
  }
}
